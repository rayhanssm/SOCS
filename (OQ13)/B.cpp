#include <stdio.h>
int main ()
{
	int n, m, i, num, a[101], b[101];
	scanf ("%d %d", &n, &m);
	for (i=0;i<n;i++)
	{
		scanf ("%d", &a[i]);
	}
	for (i=0;i<m;i++)
	{
		scanf ("%d", &b[i]);
	}
	return 0;
}
