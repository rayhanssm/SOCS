#include <stdio.h>
int main ()
{
	long long t, x, w, d, v, k, y;
	scanf ("%lld", t);
	for (int i=0;i<t;i++)
	{
		scanf ("%lld %lld %lld %lld %lld %lld", &x, &w, &d, &v, &k, &y);
		if ()
	}
	return 0;
}
